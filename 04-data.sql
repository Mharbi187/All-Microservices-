-- =============================================================================
-- NEXUS-AID — 04-data.sql
-- Placeholder: All core admin data is already seeded by 02-schema.sql.
-- Domain tables (inventory, trainers, interventions, etc.) live in nexusaiddb
-- and are managed by Spring Boot Hibernate (auto DDL).
-- =============================================================================
\c nexusaid_admin;
-- intentionally empty
